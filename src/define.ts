declare const define;

define({ 
    // utils functions
    shouldAnimate,
    getDeltaCoordinates,

    // animation functions
    slideAnimation,
    showScreenCenterAnimation,
    pauseAnimation,
    cumulatedAnimations,
 });
